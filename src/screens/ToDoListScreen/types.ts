import { NativeStackScreenProps } from '@react-navigation/native-stack';
import { RootStackParamList } from '../../navigators/types';

export type TToDoListScrenProps = NativeStackScreenProps<
  RootStackParamList,
  'ToDoListScreen'
>;
